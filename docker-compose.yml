version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: deportesdb_app
    volumes:
      - ./Vizualize/output:/app/Vizualize/output
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8000:8000"
    restart: unless-stopped